---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Both apps start without errors via turbo dev"
    - "Express API responds to GET /health with 200 JSON { ok: true }"
    - "Next.js frontend loads at http://localhost:3000 with the placeholder page"
    - "turbo test passes GREEN across all packages"
    - "turbo typecheck passes with zero errors across all packages"
  artifacts:
    - path: "apps/api/src/index.ts"
      provides: "Running Express server on port 3001"
    - path: "apps/web/app/page.tsx"
      provides: "Placeholder Next.js page at localhost:3000"
  key_links:
    - from: "turbo dev"
      to: "apps/api (port 3001) + apps/web (port 3000)"
      via: "Turborepo parallel dev task"
      pattern: "both processes start without exit"
---

<objective>
Verify the complete Phase 1 foundation is working: both apps start, tests pass, and TypeScript strict mode is clean across all packages.

Purpose: Phase 1 gates all downstream work. Before proceeding to Phase 2 (PDF parsing), the monorepo must be confirmed functional end-to-end. This checkpoint surfaces any integration issues between the workspace, types, and app shells that automated checks might miss.
Output: Human confirmation that both apps start and the full test + typecheck suite is green.
</objective>

<execution_context>
@/Users/yannis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test and typecheck suite</name>
  <files></files>
  <action>
Run the complete automated verification suite to confirm all packages are green before the human startup check:

```bash
# Run all tests across the monorepo
turbo test

# Run TypeScript strict mode check across all packages
turbo typecheck
```

Both commands must complete with zero failures. If either fails, fix the issue before proceeding to the checkpoint task.

Expected output:
- `turbo test` — @resume/types tests GREEN (8 tests), @resume/api tests GREEN (4 tests)
- `turbo typecheck` — zero TypeScript errors across @resume/types, @resume/api, @resume/web
  </action>
  <verify>
    <automated>cd /Users/yannis/Documents/Resume-Tailoring-Engine && turbo test && turbo typecheck</automated>
  </verify>
  <done>
    turbo test passes with all tests GREEN.
    turbo typecheck passes with zero errors across all three packages.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm both apps start via turbo dev</name>
  <action>Human verifies that both apps start cleanly and respond correctly.</action>
  <verify>
    <automated>MISSING — startup verification requires human observation</automated>
  </verify>
  <done>Human types "approved" after confirming both apps start and the health endpoint responds.</done>
  <what-built>
    Complete Phase 1 monorepo foundation:
    - Turborepo workspace with packages/types, apps/api, apps/web
    - ResumeStructure, RewrittenBullet, AnalysisResult types with Zod schemas
    - Express 5 API shell with error middleware (AppError, PdfParseError, OpenAiTimeoutError)
    - Next.js 16 web shell with placeholder page
    - TypeScript strict mode enabled root-wide
    - Full test suite GREEN (12 tests across types and api packages)
  </what-built>
  <how-to-verify>
1. Open a terminal and run from the repo root:
   ```
   turbo dev
   ```

2. Wait for both processes to start (10-15 seconds). You should see:
   - `API listening on http://localhost:3001` (from apps/api)
   - `Ready on http://localhost:3000` (from apps/web, Next.js)

3. Verify the API health endpoint:
   ```
   curl http://localhost:3001/health
   ```
   Expected: `{"ok":true}`

4. Open http://localhost:3000 in a browser.
   Expected: Placeholder page with "Resume Tailoring Engine" heading and "Coming soon: Step 1" text.

5. Press Ctrl+C to stop turbo dev.

If either app fails to start, note the error message in your response.
  </how-to-verify>
  <resume-signal>Type "approved" if both apps start and respond correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. turbo test — all tests GREEN (12 tests: 8 types + 4 api error middleware)
2. turbo typecheck — zero errors across @resume/types, @resume/api, @resume/web
3. Human confirmed: Express responds to GET /health with {"ok":true}
4. Human confirmed: Next.js placeholder loads at http://localhost:3000
</verification>

<success_criteria>
- turbo test passes GREEN across all packages
- turbo typecheck passes with zero TypeScript errors
- Both apps start without errors via turbo dev
- Express GET /health returns {"ok":true}
- Next.js placeholder page loads at localhost:3000
- Phase 1 is complete; Phase 2 (PDF Parsing) is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`

Then update .planning/STATE.md:
- Current Position: Phase 2
- Last activity: Phase 1 complete — monorepo, shared types, app shells in place
- Progress: [██░░░░░░░░] 14%
</output>
