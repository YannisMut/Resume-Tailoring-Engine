# Required in production — internal URL of the API service
# In Railway, use the private networking URL: http://api.railway.internal:3001
# Locally, this defaults to http://localhost:3001 if not set
API_URL=http://localhost:3001
